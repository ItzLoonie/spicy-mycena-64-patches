diff --git a/src/game/behaviors/coin.inc.c b/src/game/behaviors/coin.inc.c
index f8299546..0e3a9c57 100644
--- a/src/game/behaviors/coin.inc.c
+++ b/src/game/behaviors/coin.inc.c
@@ -137,6 +137,7 @@ void bhv_coin_init(void) {
     o->oMoveAngleYaw = random_u16();
     cur_obj_set_behavior(bhvYellowCoin);
     obj_set_hitbox(o, &sYellowCoinHitbox);
+	o->oDamageOrCoinValue = coinValue;
     if (o->apCoinSourceKind == 2) {
         s32 slotCount = 1;
         if (obj_has_behavior(o->parentObj, bhvThreeCoinsSpawn)) {
@@ -150,7 +151,10 @@ void bhv_coin_init(void) {
         }
     }
     SM64AP_MarkSpentPermanentCoin(o, coinValue);
-    cur_obj_become_intangible();
+
+    // Auto-collect: coins spawned from objects go straight to Mario
+    // instead of waiting for him to walk over them.
+    bhv_coin_collect_without_contact();
 }
 
 void bhv_coin_loop(void) {
@@ -348,8 +352,6 @@ void coin_inside_boo_act_1(void) {
 }
 
 void coin_inside_boo_act_0(void) {
-    s16 sp26;
-    f32 sp20;
     struct Object *parent = o->parentObj;
     cur_obj_become_intangible();
     if (o->oTimer == 0 && gCurrLevelNum == LEVEL_BBH) {
@@ -362,12 +364,11 @@ void coin_inside_boo_act_0(void) {
             SM64AP_AssignPermanentCoinSlot(o, parent, 0, 5);
         }
         SM64AP_MarkSpentPermanentCoin(o, 5);
-        o->oAction = 1;
-        sp26 = gMarioObject->oMoveAngleYaw;
-        sp20 = 3.0f;
-        o->oVelX = sins(sp26) * sp20;
-        o->oVelZ = coss(sp26) * sp20;
-        o->oVelY = 35.0f;
+
+        // Auto-collect: coins spawned from objects go straight to Mario
+        // instead of waiting for him to walk over them.
+        o->oDamageOrCoinValue = 5;
+        bhv_coin_collect_without_contact();
     }
 }
 
diff --git a/src/game/behaviors/moving_coin.inc.c b/src/game/behaviors/moving_coin.inc.c
index 09df1b87..5ddf84a4 100644
--- a/src/game/behaviors/moving_coin.inc.c
+++ b/src/game/behaviors/moving_coin.inc.c
@@ -148,6 +148,10 @@ void bhv_moving_yellow_coin_init(void) {
         return;
     }
     SM64AP_MarkSpentPermanentCoin(o, 1);
+
+    // Auto-collect: coins spawned from objects go straight to Mario
+    // instead of waiting for him to walk over them.
+    moving_coin_collect_without_contact();
 }
 
 void bhv_moving_yellow_coin_loop(void) {
@@ -259,6 +263,10 @@ void bhv_blue_coin_sliding_jumping_init(void) {
     o->oBuoyancy = 1.5;
 
     obj_set_hitbox(o, &sMovingBlueCoinHitbox);
+
+    // Auto-collect: coins spawned from objects go straight to Mario
+    // instead of waiting for him to walk over them.
+    moving_coin_collect_without_contact();
 }
 
 void blue_coin_sliding_away_from_mario(void) {
